overwrite: true
schema: "https://actors-crossover-gql.herokuapp.com/graphql"
# schema: "http://localhost:4000/graphql" # for local development
documents: "src/queries.ts"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
